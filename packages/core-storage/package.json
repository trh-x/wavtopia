{
  "name": "@wavtopia/core-storage",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "types/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist types",
    "dev": "tsc -w",
    "lint": "eslint src --ext .ts",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "postinstall": "pnpm migrate && pnpm generate"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "minio": "^7.1.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "prisma": "^5.7.0",
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}
